%*********************************************************************** 
%									 
%	-- Used by "filterData.m" to make final selection of desired data
%
%	-> Usage = 
%		-> newData = selectFilterData(data,selectionVector)
%
%	-> inputs =
%		-> newData - STRUCT with selected data
%		-> selectionVector - vector of zeros and ones
%									 
%	-> MATLAB version used:	
%		- R2012b (8.0.0.783) 64-bit	
%				 
% 	-> Special toolboxes used: 
%		-- none	--		
%
% 	-> Other dependencies:  
%		-- none	--		
%									 
%	-> Created by Evandro Bernardes	 								 
%		- at ENSTA Bretagne (Brest, Britanny, France)							 								 
%		- In association with: 
%			ANFR - Agence Nationale de Fréquence    		 
%									 
% 	Code version:	1						 
%	last edited in:	29/07/2016 					 
%									 
%***********************************************************************

function newData = selectFilterData(data,selectionVector)
    newPow = data.pow(selectionVector);
    newAtt = data.att(selectionVector);
    
    newYear = data.time.year(selectionVector);
    newMonth = data.time.month(selectionVector);
    newDay = data.time.day(selectionVector);
    newHour = data.time.hour(selectionVector);
    newMinute = data.time.minute(selectionVector);
    newSecond = data.time.second(selectionVector);
end